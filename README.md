# Настройка проекта

```shell
    pip install -r requirements.txt
```

Создать файл ``.env`` в корневой директории проекта и определить там переменные:
* ``URL`` - адрес сервера
* ``TOKEN`` - токен бота выданный вк